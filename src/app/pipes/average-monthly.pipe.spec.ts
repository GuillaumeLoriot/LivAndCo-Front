import { AverageMonthlyPipe } from './average-monthly.pipe';

describe('AverageMonthlyPipe', () => {
  it('create an instance', () => {
    const pipe = new AverageMonthlyPipe();
    expect(pipe).toBeTruthy();
  });
});
